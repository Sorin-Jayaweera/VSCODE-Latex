

$$
\begin{align}
V \propto e^{\frac{-\tau}{\tau_{p }} }
\end{align}
$$
$\frac{\tau}{\tau_{p}}$ has no units, time

$v$ has no units, is ratio of intensities


$$
\begin{align}
\ln(V) \propto \frac{-\tau}{\tau_{p} }=\underbrace{  -\tau }_{ \text{ x axis is scaled tau } } \underbrace{ \left( \frac{1}{\tau_{p} } \right) }_{ \text{ slope } }
\end{align}
$$
position of movable mirror is time delay is tau

x axis is scaled tau
slope is $\frac{1}{\tau _{p}}$



# quantum
$$
\begin{align} \\
\ket{H} &  = \frac{1}{\sqrt[]{ 2 } } (\ket{D} +\ket{A}) \\
\ket{V} &  = \frac{1}{\sqrt[]{ 2 } } (\ket{D} -\ket{A} ) 
\\
\frac{1}{2}(\ket{H}_{s}\ket{H}_{i} + \ket{V}_{s}\ket{V}_{i})  & = \frac{1}{\sqrt[]{ 2 } } (\ket{D} _{s}\ket{D}_{i} + \ket{A}_{s}\ket{A}_{i}       ) \\
\frac{1}{2}(\ket{H}_{s}\ket{H}_{i} - \ket{V}_{s}\ket{V}_{i})  & = \frac{1}{\sqrt[]{ 2 } } (\ket{D} _{s}\ket{A}_{i} + \ket{A}_{s}\ket{D}_{i}       )
\end{align}
$$


