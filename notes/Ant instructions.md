I want to modify the initial screen where we have the polygon.

1) I want to have 1 other page with two image panels that I can click into, the first showing just the background subtracted difference between two adjacent frames (on the frame of the sweep that the person is looking at), and the second with the identified ants of that image. I also want the relevant parameters - i.e. thresholds, track dist, blob areas, to have sliders. After the sliders are changed, rerun the detection on that single frame for the person to see how well it did.

2) on the main page where we draw the polygon, I want to instead draw a circle. I want to click the center and then move my mouse around (with the circle radius following it) and click at the radius I want to set the circle. I want the circle to be divided into 4 quadrants, with a different color for each quadrant's arc line.   

3) I want to have an option to select the root folder, and then see all of the videos  within that folder. All videos are in that root folder. I want the output to be saved in a folder with the same name as the video,  with the tracking video, the csv of timestamps, and the csv containing the circle center and radius. 

4) I want to be able to either select one video from the base folder and analyze that, or click multiple checkmarks for videos to queue up to analyze. When I do the checkmark on each new file. I want to have the image view show for that most recent video selected. If multiple videos are selected, there should be a button for all parameters shared between them, or individually set for each video. If it is individually set, then for each file I click, I should be able to move the parameter sliders and fine tune what works for that video, and then check the next video and fine tune parameters for that one. 

Do you have any questions? Otherwise, do it