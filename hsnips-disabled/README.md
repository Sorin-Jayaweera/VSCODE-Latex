HyperSnips is intentionally pointed at this empty folder in this workspace.

The active snippet engine is `Sorin Latex Suite Auto`. Keeping HyperSnips away
from the generated `.hsnips` files prevents triggers such as `dint` from
expanding twice.
